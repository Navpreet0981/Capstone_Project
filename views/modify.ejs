<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Item</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="container">
        <h1>
            <%= heading %>
        </h1>
        <% if (locals.item) { %>
             <form id="editForm" method="post" action="/api/items/<%=item.id%>">
        <input type="text" name="title" value="<%=item.title %>">
        <textarea name="content" rows="10"><%=item.content %></textarea>
        <input type="text" name="author" value="<%=item.time %>">
        <button class="full-width" type="submit">
          <%= submit %>
        </button>
      </form>
       <% }else{ %>
        <form id="newItemForm" method="post" action="/api/items">
            <input type="text" name="title" placeholder="Title" required>
            <textarea name="content" placeholder="Content" required rows="10"></textarea>
            <input type="text" name="time" placeholder="How Much Time (eg: 4-Hours)" required>
            <button class="full-width" type="submit">
              <%= submit %>
            </button>
          </form>
        <% } %>

    </div>
</body>
</html>